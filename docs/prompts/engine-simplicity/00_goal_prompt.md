/goal In /home/martinkavik/repos/boon-dd, implement BOON_DD_ENGINE_SIMPLICITY_PLAN.md end to end. Treat that file as the source of truth, alongside BOON_DD_HONEST_COMPILER_PLAN.md for compiler correctness.

The goal is a simple, fast, honest pure Timely/Differential Dataflow Boon engine. Add the required xtask command surface and deterministic failing gates first, then fix the implementation phase by phase until the gates pass. Do not fake success, do not weaken gates to hide defects, and do not commit or push unless I explicitly ask.

Remove fixture-dispatch and shortcut execution paths from runtime backends and generated execution. In particular, eliminate or make non-execution-path all paths that select checked-in generated crates by example name/source path/source text, call boon_examples::run_generated_for_checked_source from runtime backends, route source events by all non-host events instead of compiler source ids, discard dynamic owner/generation identity, rebuild/replay generated graphs per interaction, clone full output vectors on execution paths, evaluate Boon/list/text/library semantics in host-side Rust helpers, or verify browser/native behavior through smoke strings/precomputed JSON instead of generated Timely/DD graph execution.

Do the work in the order required by BOON_DD_ENGINE_SIMPLICITY_PLAN.md: Phase 0 gates and reports, no-fixture-dispatch path, source-id routing, dynamic owner/generation routing, DD stateful lowering, long-lived runtime sessions, incremental output drains, stress and human-style interaction proof, complexity/code-size budgets, stale-artifact/dd-purity verification, prompt audits, and final aggregate verification.

Any GUI/browser/native verification command must wrap the actual window-creating process with cosmic-background-launch --workspace boon-dd -- ... so the launch context is inherited correctly.

Do not mark the goal complete until cargo xtask verify all --format json passes on the current checkout and writes target/boon-artifacts/success.json plus every required BOON_DD_ENGINE_SIMPLICITY_PLAN.md artifact with engine_simplicity verdict pass, dd_purity verdict pass, prompt-audit verdict pass, zero fixture dispatch execution paths, correct source-routing negative checks, zero dynamic owner leaks, one graph build per multi-interaction session, zero full-output-vector clones in execution paths, stateful DD lowering checks pass, stress structural checks pass, no stale artifacts, and Firefox proof that the generated Timely/DD graph runs in the browser process.

If the full goal is blocked, do not fake success. Write a checked-in blocker report under docs/blockers/ with the exact failing command, output, current artifact paths, minimized repro, current git head and dirty status, and the next pin/fork/fix decision.

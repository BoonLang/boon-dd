# Ply Renderer Review: old-renderer-removal

Search for old renderer leftovers including app_window, boon_backend_wgpu, direct render pipelines, shader modules, custom vector/glyph raster code, inline DOM-created app controls, browser-webgpu, wgpu-command-schema, and browser-webgpu-command-schema. Pass only if repo-owned app rendering no longer uses them outside historical plan text or generated negative fixtures.

Required report schema: reviewer, model, git_commit, deterministic_report_sha256, prompt_file, prompt_sha256, commands_run, files_examined, deterministic_artifacts_examined, findings, verdict.

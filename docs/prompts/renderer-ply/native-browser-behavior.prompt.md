# Ply Renderer Review: native-browser-behavior

Review native and Firefox browser Ply smoke artifacts. Pass only if both surfaces load every required Boon DD example, present a nonblank Ply UI, and prove at least one selection/state-changing interaction without relying on a custom JavaScript renderer.

Required report schema: reviewer, model, git_commit, deterministic_report_sha256, prompt_file, prompt_sha256, commands_run, files_examined, deterministic_artifacts_examined, findings, verdict.
